{
  "mcpServers": {
    "langwatch": {
      "command": "npx",
      "args": ["-y", "@langwatch/mcp-server"],
      "env": {
        "LANGWATCH_API_KEY": "${LANGWATCH_API_KEY}",
        "LANGWATCH_ENDPOINT": "${LANGWATCH_ENDPOINT}"
      }
    }
  }
}
